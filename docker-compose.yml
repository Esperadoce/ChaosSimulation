services:
  app:
    image: esperadoce/chaos-simulation:${TAG:-latest}
    container_name: chaos-simulation
    ports:
      - "12908:12908"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
