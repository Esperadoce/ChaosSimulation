version: '3.8'
services:
  app:
    # Use your registry + repo name here:
    image: esperadoce/chaos-simulation:${TAG:-latest}
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    # volumes:
    #   - ./nginx.conf:/etc/nginx/nginx.conf:ro
